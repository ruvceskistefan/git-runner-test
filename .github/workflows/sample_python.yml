name: Sample Python

on: 
  # Allows you to run this workflow manually from the Actions tab
  workflow_dispatch:

jobs:
  build:
    runs-on: [self-hosted]
    steps:
    - name: checkout repo content
      uses: actions/checkout@v2
    - name: setup python
      uses: actions/setup-python@v2
    - name: Simple python test
      shell: python
      run: |
        print("Hello world from Python!!! Wuhuuu!")